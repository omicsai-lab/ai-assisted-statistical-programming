# Scripts

The scripts are intentionally written as plain `.R` and `.py` files rather than notebooks. This makes them easy to run from top to bottom, test automatically, and compare across languages.

Run all Python examples from the repository root:

```bash
python run_all_python_examples.py
```

Run all R examples from the repository root:

```bash
Rscript run_all_R_examples.R
```
